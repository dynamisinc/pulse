name: Deploy Frontend

# Builds the Vite SPA and publishes it to the Static Web App (stapp-pulse-uat).
# Pulse has no separate CI workflow yet, so this workflow runs its own quality
# gates (lint + type-check + test) before deploying.

on:
  push:
    branches: [main]
    paths:
      - "src/frontend/**"
      - ".github/workflows/deploy-frontend.yml"
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "uat"
        type: choice
        options:
          - uat

env:
  NODE_VERSION: "22"

concurrency:
  group: deploy-frontend-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build Frontend
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment || 'uat' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: src/frontend

      - name: Lint
        run: npm run lint
        working-directory: src/frontend

      - name: Type-check
        run: npm run type-check
        working-directory: src/frontend

      - name: Test
        run: npm run test:run
        working-directory: src/frontend

      - name: Build
        run: npm run build
        working-directory: src/frontend
        env:
          # Optional. Blank runs the SPA against mock data only (see validateEnv.ts).
          VITE_API_URL: ${{ vars.VITE_API_URL || '' }}

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: frontend
          path: src/frontend/dist
          retention-days: 1

  deploy:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: ${{ github.event.inputs.environment || 'uat' }}

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend
          path: src/frontend/dist

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          skip_app_build: true
          skip_api_build: true
          app_location: "src/frontend/dist"
